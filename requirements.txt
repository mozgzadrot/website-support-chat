# Core web framework
flask==3.1.0
Werkzeug==3.1.3

# HTTP client (for OpenRouter streaming)
requests==2.32.3

# NLP / embeddings
sentence-transformers==3.3.1
torch==2.5.1
numpy==1.26.4

# HTML parsing (for indexer)
beautifulsoup4==4.12.3

# Environment config
python-dotenv==1.0.1

# Optional: sqlite-vec extension loader (graceful fallback if unavailable)
# sqlite-vec==0.1.6   # Uncomment if sqlite-vec is available on your platform
